# Unselected tree examples for the running example: the parse trees the user rejected.
# Each line is `example TOP BOT INDEX`, where TOP and BOT are production identifiers
# (see `greta cfg2ta`) and INDEX is the child position of the nested symbol.
example 5 5 2
example 6 6 2
example 6 5 0
example 1 2 3
